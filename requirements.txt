# Runtime deps for the API + digest (on top of the scrapers' own needs:
# beautifulsoup4 is only used by the Eventbrite scraper).
fastapi>=0.110
uvicorn>=0.29
streamlit>=1.33
pandas>=2.0
requests>=2.31
